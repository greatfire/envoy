From: h <h@localhost>
Date: Wed, 01 Jul 2020 18:13:10 +0000
Subject: [PATCH 1/1] Disable external intent(app link)

 components/external_intents/android/java/src/org/chromium/components/external_intents/ExternalNavigationHandler.java | 3 +++
 1 file changed, 3 insertions(+)

--- a/components/external_intents/android/java/src/org/chromium/components/external_intents/ExternalNavigationHandler.java
+++ b/components/external_intents/android/java/src/org/chromium/components/external_intents/ExternalNavigationHandler.java
@@ -1092,6 +1092,8 @@ public class ExternalNavigationHandler {
     }
 
     private boolean launchExternalIntent(Intent targetIntent, boolean shouldProxyForInstantApps) {
+       return false;
+       /*
         try {
             if (!startActivityIfNeeded(targetIntent, shouldProxyForInstantApps)) {
                 if (DEBUG) Log.i(TAG, "The current Activity was the only targeted Activity.");
@@ -1105,6 +1107,7 @@ public class ExternalNavigationHandler {
         }
         if (DEBUG) Log.i(TAG, "startActivityIfNeeded");
         return true;
+       */
     }
 
     // This will handle external navigations only for intent meant for Autofill Assistant.
